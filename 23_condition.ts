// Question 23

let car: string = 'subaru';

console.log("Is car == 'subaru'? I predict true.");
console.log(car == 'subaru');

console.log("Is car == 'suzuki'? I predict false.");
console.log(car == 'suzuki');

console.log("Is car != 'civic'? I predict true.");
console.log(car != 'civic');

console.log("Is car != 'subaru'? I predict false.");
console.log(car != 'subaru');

console.log("Is car === 'subaru'? I predict true.");
console.log(car === 'subaru');

console.log("Is car === 'Subaru'? I predict false.");
console.log(car === 'Subaru');

console.log("Is car !== 'honda'? I predict true.");
console.log(car !== 'honda');

console.log("Is car !== 'subaru'? I predict false.");
console.log(car !== 'subaru');

console.log("Is car.length > 5? I predict false.");
console.log(car.length > 5);

console.log("Is car.length < 10? I predict true.");
console.log(car.length < 10);

console.log("Is car.charAt(0) === 's'? I predict true.");
console.log(car.charAt(0) === 's');

// In this code, we define a variable car and assign it the value 'subaru'.
// We then perform a series of conditional tests using the comparison operators (==, !=, ===,<,>, and !==) 
// to compare the value of car with different strings.

// For each test, we print a statement describing the test and our prediction for the result.

// == (Equality): The == operator checks for equality between two values,

// != (Inequality): The != operator checks for inequality between two values,

// === (Strict Equality): The === operator checks for strict equality between two values without performing 
// type coercion. It compares both the values and their types. 
// For example, '1' === '1' would be considered true because the values and types are identical.

// < (Less Than): The < operator checks if the value on the left side is less than the value on the right side.

// > (Greater Than): The > operator checks if the value on the left side is greater than the value 
// on the right side.

// !== (Strict Inequality): The !== operator checks for strict inequality between two values 
// without performing type coercion. It is the negation of the === operator. 
// For example, '1' !== 1 would be considered true because the values and types are not identical.

