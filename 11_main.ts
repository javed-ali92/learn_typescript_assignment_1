// Question 11

var members = ['javed', 'tayyab', 'sibtain', 'mishkat'];
for (var i = 0; i < members.length; i++) {
    console.log(members[i]);
}

// In this code, we have an array called names that stores the names of my friends.

// We then use a for loop to iterate over each element in the names array. 
// The loop prints each person's name by accessing each element using the index i and using console.log().